# homebrew-tap

Homebrew formulae and casks for [binsight](https://github.com/adrijshikhar/binsight) —
a local MySQL & MariaDB binlog viewer and analyzer.

```sh
brew install adrijshikhar/tap/binsight
```

Casks here are published automatically by GoReleaser on each binsight release.
